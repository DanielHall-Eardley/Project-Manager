import React, { Component } from 'react';
import {Route, Switch, Link} from 'react-router-dom';
import './App.css';

class Budget extends Component {
  render() {
    return (
      <div>
        {/*This page will have a list of all budget items,
         which will be compared to actual cost of items to check if 
         over/under budget. The working groups with also have 
         their allocated budgets and sublists of budget items.*/}
      </div>
    );
  }
}

export default Budget;